---
wave: 1
depends_on: []
files_modified:
  - package.json
  - src/store/useStore.ts
  - src/types.ts
autonomous: true
requirements: [REQ-2]
---

# Plan 02-01: Foundation — Dependencies + Store Extension

## Objective
Install the 5 missing runtime packages and 1 dev type package, then extend useStore.ts and types.ts with jogPos state.

## Tasks

<task id="T1" name="install-dependencies">
  <read_first>package.json</read_first>
  <action>
Run from the project root:

```
npm install @use-gesture/react @react-spring/web @react-three/fiber @react-three/drei three
npm install -D @types/three
```

Confirm peer-dep resolution succeeds with no unresolvable conflicts (React 19 satisfies the `>=18` peer dep declared by react-spring 9 and R3F 8).
  </action>
  <acceptance_criteria>
  - `package.json` dependencies block contains `@use-gesture/react`, `@react-spring/web`, `@react-three/fiber`, `@react-three/drei`, `three`
  - `package.json` devDependencies block contains `@types/three`
  - `node_modules/@use-gesture/react` exists on disk
  - `node_modules/three` exists on disk
  - `npm run build` exits with code 0 after install (no peer-dep errors in output)
  </acceptance_criteria>
</task>

<task id="T2" name="extend-store-jogpos">
  <read_first>src/store/useStore.ts</read_first>
  <action>
Edit `src/store/useStore.ts`:

1. Add `jogPos: number` field to `StoreState` interface with value `0`.
2. Add `setJogPos: (v: number) => void` method to `StoreState` interface.
3. Inside the `create` call, add `jogPos: 0` to the initial state object.
4. Add the implementation: `setJogPos: (v) => set({ jogPos: v })`.

Keep all existing fields and methods unchanged. The final interface shape:

```ts
interface StoreState {
  // ...existing fields...
  jogPos: number;
  setJogPos: (v: number) => void;
}
```
  </action>
  <acceptance_criteria>
  - `src/store/useStore.ts` exports `useStore` with `jogPos` typed as `number`
  - `src/store/useStore.ts` exports `useStore` with `setJogPos` typed as `(v: number) => void`
  - `npx tsc --noEmit` exits 0 (no TypeScript errors)
  - Calling `useStore.getState().setJogPos(0.5)` then `useStore.getState().jogPos` returns `0.5` (verifiable via a vitest `it` block or quick manual check in dev console)
  </acceptance_criteria>
</task>

<task id="T3" name="verify-build-clean">
  <read_first>src/types.ts</read_first>
  <action>
Inspect `src/types.ts` — confirm `WayoState`, `SceneId`, and `Zone` types are already defined. No new types are needed in this plan; jogPos is a plain `number`.

Run the full build and test suite to confirm the foundation is clean before Wave 2 work begins:

```
npm run build
npm test
```

If either command fails, fix the root cause before marking T3 complete. Do not proceed to Wave 2 plans while the build is broken.
  </action>
  <acceptance_criteria>
  - `npm run build` exits 0 with no TypeScript errors and no Vite bundle errors
  - `npm test` exits 0 (all 27 existing Vitest tests pass; no regressions)
  - `src/types.ts` requires no modification for this wave (jogPos is a number, not a union type)
  </acceptance_criteria>
</task>

## must_haves
- truths:
  - All 6 packages are installed before any Wave 2 file is written
  - `useStore` exports `jogPos: number` (default `0`) and `setJogPos: (v: number) => void`
  - Build and tests are green after this plan completes

## Artifacts this phase produces
- `package.json` — adds `@use-gesture/react`, `@react-spring/web`, `@react-three/fiber`, `@react-three/drei`, `three`, `@types/three`
- `src/store/useStore.ts` — extended with `jogPos` + `setJogPos`
