---
phase: 03-hub-polish-cinematics
plan: "04"
type: execute
wave: 4
depends_on:
  - "03-02"
files_modified:
  - src/components/three/PostProcessing.tsx
  - src/components/three/GlobalScene.tsx
autonomous: true
requirements:
  - P3-POST

must_haves:
  truths:
    - "ACES Filmic tone mapping applies to all 3D content"
    - "Subtle bloom glow visible on emissive materials (rings, token)"
    - "Vignette darkens screen edges, noise adds film grain"
    - "All DOM overlay text remains unaffected by post-processing"
    - "Low-GPU devices get resolutionScale=0.5 with bloom disabled"
  artifacts:
    - src/components/three/PostProcessing.tsx
    - src/components/three/GlobalScene.tsx (add PostProcessing import)
  key_links:
    - "EffectComposer renders last in GlobalScene tree (after all scene content)"
    - "EffectComposer auto-sets renderer.toneMapping = NoToneMapping"
    - "Emissive materials use toneMapped=false (set in Plans 03-03, 03-05)"
---

## Post-Processing — ACES + Bloom + Vignette + Noise

<objective>
Apply broadcast-grade post-processing stack to all 3D content in the global Canvas:
ACES Filmic tone mapping, subtle bloom, vignette, and film grain noise.
Include performance fallback for low-GPU devices.
</objective>

<execution_context>
@C:/Users/anoop/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/anoop/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-hub-polish-cinematics/03-RESEARCH.md (Pattern 4, Pitfall 3, Open Question 1)
@.planning/phases/03-hub-polish-cinematics/03-CONTEXT.md (Post-processing & Aesthetic)
@src/components/three/GlobalScene.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: PostProcessing component with full effect chain</name>
  <files>src/components/three/PostProcessing.tsx</files>
  <action>
Create PostProcessing.tsx per RESEARCH.md Pattern 4 exact code template.

Imports:
  EffectComposer, ToneMapping, Bloom, Vignette, Noise from @react-three/postprocessing
  ToneMappingMode from postprocessing

Effect chain (in order — order matters for correct compositing):
  1. ToneMapping mode={ToneMappingMode.ACES_FILMIC}
  2. Bloom: luminanceThreshold={0.2}, luminanceSmoothing={0.9}, intensity={0.4}, mipmapBlur
  3. Vignette: eskil={false}, offset={0.15}, darkness={0.6}
  4. Noise: opacity={0.015}

EffectComposer props: multisampling={0} (MSAA handled internally by postprocessing lib).

CRITICAL per RESEARCH.md Pitfall 3: EffectComposer forces renderer.toneMapping = NoToneMapping automatically. DOM overlay text is unaffected (it is HTML, not WebGL). 3D content readability: emissive materials on rings/tokens use toneMapped=false (already set in Plan 03-03). Any future 3D labels must also use toneMapped=false or stay in DOM layer.

ACES curve reference: maps HDR to LDR with cinematic S-curve — shadows crushed slightly, highlights compressed, mid-tones preserved. This gives the "F1 broadcast" look specified in CONTEXT.md.
  </action>
  <verify>
    <automated>MISSING — manual visual check; open app, confirm ACES tonemap visible (blacks deeper, highlights cinematic), bloom glow on ring emissive, vignette darkens corners, slight film grain</automated>
  </verify>
  <done>
PostProcessing.tsx exports component with EffectsComposer wrapping ToneMapping/Bloom/Vignette/Noise. Wired into GlobalScene (Task 2). Visual check confirms F1 broadcast aesthetic.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire PostProcessing into GlobalScene + GPU fallback</name>
  <files>src/components/three/GlobalScene.tsx, src/components/three/PostProcessing.tsx</files>
  <action>
A. Wire into GlobalScene:

Import PostProcessing. Add <PostProcessing /> as LAST child of GlobalScene fragment — after CameraRig, HubOrbitMap, and CloudScene (when added). Post-processing must be applied after all scene content renders.

B. Implement GPU tier detection + fallback per RESEARCH.md Open Question 1:

In PostProcessing.tsx, add a useDetectGPU() hook (inline, no external dep):

function useDetectGPU(): 'high' | 'low' {
  // Check hardwareConcurrency as proxy for device tier
  const cores = navigator.hardwareConcurrency || 4;
  // Also check WebGL renderer info if available via useThree
  return cores < 4 ? 'low' : 'high';
}

If tier is 'low':
- EffectComposer resolutionScale={0.5}
- Bloom disabled (don't render Bloom component)
- Vignette and Noise kept (cheap, single-pass)
- ToneMapping kept (essential for color correctness)

If tier is 'high':
- Full effect chain as specified in Task 1

Use React conditional rendering: {tier === 'high' && <Bloom ... />}. Do NOT use dynamic import — effects load at component mount.

This addresses RESEARCH.md Open Question 1: minimum viable GPU tier detection + graceful degradation.
  </action>
  <verify>
    <automated>MISSING — manual check: devtools Performance tab, verify single EffectComposer pass, no double tone-mapping artifacts</automated>
  </verify>
  <done>
Post-processing applies to all 3D content. Bloom visible on emissive materials. Vignette darkens edges. Film grain subtle. DOM overlay text unaffected. GPU fallback: low-tier devices get half-res + no bloom.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| EffectComposer -> GPU | Post-processing shaders execute on GPU; no user input reaches shader uniforms |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-04a | Denial of Service | EffectComposer on low-GPU | medium | mitigate | resolutionScale=0.5 + bloom disabled when hardwareConcurrency < 4 per Open Question 1 recommendation |
| T-03-04b | Tampering | Shader uniform injection | low | accept | No user-controlled shader uniforms in this app. All effect parameters are hardcoded constants. RESEARCH.md security domain confirms this. |
</threat_model>

<verification>
Manual visual: ACES filmic look confirms tone mapping. Bloom glows on emissive rings. Vignette darkens corners. DOM text readable — no double tone-mapping.
</verification>

<success_criteria>
ACES Filmic tone mapping active. Bloom on emissive materials. Vignette + noise present. DOM overlay text crisp. GPU fallback works on low-tier devices.
</success_criteria>

<output>
Create .planning/phases/03-hub-polish-cinematics/03-04-SUMMARY.md when done
</output>
