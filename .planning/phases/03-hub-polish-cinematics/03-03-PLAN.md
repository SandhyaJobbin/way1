---
phase: 03-hub-polish-cinematics
plan: "03"
type: execute
wave: 3
depends_on:
  - "03-02"
files_modified:
  - src/components/three/OrbitRing.tsx
  - src/components/three/HubOrbitMap.tsx
  - src/components/three/IncidentToken.tsx
  - src/components/three/GlobalScene.tsx
autonomous: true
requirements:
  - P3-ORBIT

must_haves:
  truths:
    - "Hub view shows 3 concentric TorusGeometry rings lying flat on XZ plane"
    - "Live ring (#FF6B2B) at radius 3.5, Response ring (#E8E8E8/#CC0000) at 7.0, Learning ring (#4A90E2/#7B61FF) at 10.5"
    - "Active ring pulses emissiveIntensity via react-spring (no setState in useFrame)"
    - "IncidentToken sphere with pointLight orbits current active ring"
  artifacts:
    - src/components/three/OrbitRing.tsx
    - src/components/three/HubOrbitMap.tsx
    - src/components/three/IncidentToken.tsx
    - src/components/three/GlobalScene.tsx (add conditional HubOrbitMap)
  key_links:
    - "HubOrbitMap renders OrbitRing x3 + IncidentToken"
    - "GlobalScene conditionally renders HubOrbitMap when cameraTarget === 'hub'"
    - "react-spring/three useSpring animates emissiveIntensity and scale (no useFrame setState)"
---

## Orbital Map — Concentric Rings + Incident Token

<objective>
Build the ecosystem orbital map: three concentric TorusGeometry rings with color-coded
zone accents, react-spring pulse animations, and an IncidentToken that orbits the active ring.
Conditionally render inside GlobalScene when cameraTarget is hub.
</objective>

<execution_context>
@C:/Users/anoop/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/anoop/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-hub-polish-cinematics/03-RESEARCH.md (Pattern 3, Code Examples)
@.planning/phases/03-hub-polish-cinematics/03-CONTEXT.md (Orbital Map Geometry, Incident Token)
@src/components/three/GlobalScene.tsx
@src/store/useStore.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: OrbitRing — single animated TorusGeometry ring</name>
  <files>src/components/three/OrbitRing.tsx</files>
  <behavior>
    - Ring renders as flat TorusGeometry (rotation [PI/2, 0, 0]) at given radius
    - Color and emissive match props; toneMapped=false preserves color accuracy
    - When active=true, emissiveIntensity pulses 0.15 -> 0.6*pulseIntensity via react-spring
    - When active=false, stays at 0.15 emissive, scale [1,1,1]
  </behavior>
  <action>
Implement OrbitRing per RESEARCH.md Pattern 3 exact code template:

Props: radius (number), color (string), active (boolean), pulseIntensity (number, default 1).

Use @react-spring/three: animated.mesh wraps TorusGeometry + animated.meshStandardMaterial. useSpring drives emissiveIntensity and scale.

MeshStandardMaterial config: color=color, emissive=color, transparent=true, opacity=0.85, toneMapped=false.

TorusGeometry args: [radius, 0.08, 16, 100] — thin tube (0.08) with 16 radial segments, 100 tubular for smooth circle.

CRITICAL: toneMapped=false on the material per RESEARCH.md Pitfall 3 — ACES tone mapping (Plan 03-04) handles the full pipeline; emissive materials must bypass renderer tone mapping to maintain color accuracy.

Do NOT use useFrame. Do NOT use setState inside animation loops. react-spring/three manages its own frame loop outside React's scheduler.
  </action>
  <verify>
    <automated>npx vitest run -t "ring colors"</automated>
  </verify>
  <done>
OrbitRing renders without errors. Receives radius/color/active props. Active ring pulses emissiveIntensity. Inactive ring stays dim. toneMapped=false on material. No useFrame usage. Test from Plan 03-01 no longer skipped — it passes or is replaced with real assertions.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: HubOrbitMap + IncidentToken — compose rings with animated token</name>
  <files>src/components/three/HubOrbitMap.tsx, src/components/three/IncidentToken.tsx</files>
  <behavior>
    - HubOrbitMap renders 3 OrbitRing components with correct radius/color per RESEARCH.md ring specs table
    - IncidentToken renders as sphere at position on active ring with pointLight
    - activeRing prop controls which ring (1|2|3) the token orbits
    - Token orbits continuously in idle; uses useFrame with ref mutation (no setState)
  </behavior>
  <action>
A. Create HubOrbitMap.tsx:

Renders 3 OrbitRing components inside a group rotated flat. Bridge props from Zustand or parent:
- Live ring: radius=3.5, color="#FF6B2B", active=(cameraTarget==='zone1')
- Response ring: radius=7.0, color="#E8E8E8", active=(cameraTarget==='zone2')
- Learning ring: radius=10.5, color="#4A90E2", active=(cameraTarget==='zone3')
- activeRing: 1 for zone1, 2 for zone2, 3 for zone3, default 1

Also render a center pulsing WayoToken (small sphere at origin, emissive #FFFFFF, emissiveIntensity 0.1->0.5 pulse via react-spring, radius 0.15).

B. Create IncidentToken.tsx per RESEARCH.md Code Examples section:

Props: activeRing (1|2|3).
Arrays: angles = [0, PI*0.66, PI*1.33]; radii = [3.5, 7.0, 10.5].

Use useFrame with Mesh ref to orbit token around its ring: increment angle via ref, compute position = [cos(angle)*radii[idx], 0.3, sin(angle)*radii[idx]]. Use useRef for angle — NOT useState (Pitfall 2).

Also use react-spring for scale pulse (1.0 -> 1.2 bounce on ring change).

Sphere geometry: args=[0.2, 16, 16]. MeshStandardMaterial: color="#FF6B2B", emissive="#FF6B2B", emissiveIntensity=1.5, toneMapped=false. Attach pointLight (intensity=2, distance=3, color="#FF6B2B") as child of mesh.

Orbit speed: 0.3 rad/s — one full orbit in ~21 seconds.
  </action>
  <verify>
    <automated>npx vitest run -t "ring colors"</automated>
  </verify>
  <done>
HubOrbitMap renders 3 rings with correct colors and radii. IncidentToken visible as pulsing sphere with attached pointLight. Token orbits active ring. OrbitRing test passes.
  </done>
</task>

<task type="auto">
  <name>Task 3: Wire HubOrbitMap into GlobalScene conditionally</name>
  <files>src/components/three/GlobalScene.tsx</files>
  <action>
Update GlobalScene.tsx to conditionally render HubOrbitMap:

Add import: import { HubOrbitMap } from './HubOrbitMap';
Add zustand selector: const cameraTarget = useStore((s) => s.cameraTarget);
Add conditional: {cameraTarget === 'hub' && <HubOrbitMap />}

HubOrbitMap reads activeRing from zustand internally (mapping cameraTarget to ring index). When camera dollies to a zone, HubOrbitMap hides — rings only visible in hub view per CONTEXT.md.

The full tracer scene is now: ambientLight + CameraRig + HubOrbitMap(conditional) + awaiting CloudScene (Plan 03-05) + PostProcessing (Plan 03-04).
  </action>
  <verify>
    <automated>npx vitest run -t "canvas renders"</automated>
  </verify>
  <done>
Hub visible: 3 rings render when cameraTarget='hub'. Hub hidden: rings disappear when cameraTarget='zone1'/'zone2'. GlobalScene.test.tsx P3-01 test graduates from skip to real assertion.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| None new | Orbital map renders inside existing global Canvas — no new trust boundaries |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-03a | Denial of Service | OrbitRing/IncidentToken | low | accept | TorusGeometry is GPU-cheap (few hundred vertices). If 3 rings + token cause frame drops on low-end devices, reduce tubularSegments from 100 to 64 in Plan 03-07. |
| T-03-03b | Denial of Service | useFrame in IncidentToken | low | mitigate | useFrame only mutates a ref (angle), never calls setState. This is the R3F-recommended pattern per RESEARCH.md Pitfall 2. |
</threat_model>

<verification>
npx vitest run — OrbitRing test passes. GlobalScene renders with HubOrbitMap visible. No useFrame setState warnings.
</verification>

<success_criteria>
3 concentric rings visible at hub. Token orbits active ring. Rings hide on zone dolly.
</success_criteria>

<output>
Create .planning/phases/03-hub-polish-cinematics/03-03-SUMMARY.md when done
</output>
