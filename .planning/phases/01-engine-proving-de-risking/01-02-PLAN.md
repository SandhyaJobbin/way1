---
phase: "01-engine-proving-de-risking"
plan: "02"
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - "package.json"
  - "vite.config.ts"
  - "src/lib/scorm.ts"
  - "src/data/zones.json"
  - "src/schema/content.test.ts"
  - "src/schema/content.ts"
  - "src/store/useAppStore.ts"
  - "src/main.tsx"
autonomous: true
requirements: ["MVP-P1-01", "MVP-P1-02", "MVP-P1-03", "MVP-P1-04", "MVP-P1-05"]
must_haves:
  truths:
    - "SCORM 1.2 wrapper gracefully falls back locally and connects when inside Reach 360 LMS."
    - "Zod validates the content architecture schemas so downstream components trust the data."
    - "Global app state initialized to track SCORM connection status."
  artifacts:
    - "src/lib/scorm.ts"
    - "src/schema/content.ts"
    - "src/store/useAppStore.ts"
  key_links:
    - from: "src/lib/scorm.ts"
      to: "src/store/useAppStore.ts"
      via: "main.tsx initialization call"
---

<objective>
Setup the SCORM 1.2 connection wrapper with local fallback, initialize the Zustand global app store, and define upfront Zod validation schemas for the ecosystem/zone content.

Purpose: Derisk SCORM local vs production environments early, and prove the config data structure.
Output: A global Zustand store communicating with `pipwerks` (or console logging locally), and solid `zod` schemas describing the app's payload.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-engine-proving-de-risking/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: SCORM Wrapper & Zustand Store Initialization</name>
  <files>src/lib/scorm.ts, src/store/useAppStore.ts, src/main.tsx</files>
  <action>
    Install `zustand` and `pipwerks-scorm-api-wrapper`.
    Create `src/store/useAppStore.ts` defining state for `{ scormConnected: boolean, progress: number, currentZone: string | null }`. Include throttling mechanism (per decision D-11) for updating `progress` to prevent heavy side effects on each tick during scrubbing.
    Create `src/lib/scorm.ts` to initialize pipwerks SCORM 1.2 and expose `window.pipwerks`. If it fails (local dev), set `scormConnected` to false and console.warn gracefully. Export an init function.
    Call the SCORM init function early in `src/main.tsx` before rendering the router.
  </action>
  <verify>
    <automated>grep -q 'pipwerks' src/lib/scorm.ts &amp;&amp; grep -q 'create(' src/store/useAppStore.ts</automated>
  </verify>
  <done>SCORM initialization triggers on load without throwing terminal errors locally.</done>
</task>

<task type="auto">
  <name>Task 2: Define Content Architecture (Zod Schemas)</name>
  <files>src/schema/content.ts</files>
  <action>
    Install `zod`.
    Create `src/schema/content.ts`. Define schemas for `Ecosystem` (containing Rings like Live/Response, and Zones), `Zone` (id, title, color theme), and `Incident` (if applicable).
    Ensure the schemas match the structural expectations for dynamic routing. Export the valid types (`z.infer`).
  </action>
  <verify>
    <automated>grep -q 'z.object' src/schema/content.ts</automated>
  </verify>
  <done>Zod schemas export explicit TS types for Ecosystem and Zone.</done>
</task>


<task type="auto">
  <name>Task 3: Implement Dummy Data & Validation Tests</name>
  <files>src/data/zones.json, src/schema/content.test.ts, package.json, vite.config.ts</files>
  <action>
    Create `src/data/zones.json` with dummy data modeling the ecosystem and zones (proving the static import data architecture).
    Dynamically feed this data through the Zod schemas from Task 2 in a central initialization/validation location (or export the validated data).
    Install `vitest` as a dev dependency. Update `package.json` scripts to include `test: vitest run`.
    (Optional: if using Vitest with Vite, setup might require slight `vite.config.ts` updates).
    Write unit tests in `src/schema/content.test.ts` directly importing the schemas and validating both correct data (like `zones.json`) and intentionally incorrect data to ensure Zod catches issues.
  </action>
  <verify>
    <automated>npm run test</automated>
  </verify>
  <done>Vitest runs and validates the schema correctly according to our dummy static data.</done>
</task>
</tasks>

<verification>
- `npm run build` succeeds without TS errors.
- SCORM gracefully fails in local development instead of crashing the React parse tree.
- Store exposes `.getState()` functionality globally when needed.
</verification>

<success_criteria>
- The shell data and tracking foundation is secure before UI/3D logic begins to rely on it.
</success_criteria>

<output>
Create `.planning/phases/01-engine-proving-de-risking/01-02-SUMMARY.md` when done
</output>