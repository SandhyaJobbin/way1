---
phase: "01-engine-proving-de-risking"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - "package.json"
  - "vite.config.ts"
  - "src/main.tsx"
  - "src/App.tsx"
  - "src/routes.tsx"
  - "src/components/StartGate.tsx"
  - "src/components/Hub.tsx"
  - "src/components/ZonePlaceholder.tsx"
  - "src/layouts/ShellLayout.tsx"
autonomous: true
requirements: ["MVP-P1-01", "MVP-P1-02", "MVP-P1-03", "MVP-P1-04", "MVP-P1-05"]
must_haves:
  truths:
    - "The application can be launched directly from a local zipped folder without requiring a web server to resolve assets."
    - "Users do not encounter 'route not found' or 404 errors when reloading the page inside an LMS iframe."
    - "Visual transitions between the Start, Hub, and Zone screens occur smoothly without full page flashes or hard reloads."
  artifacts:
    - "vite.config.ts"
    - "src/routes.tsx"
  key_links:
    - "main.tsx successfully renders HashRouter to the DOM."
---

<objective>
Initialize the vertical empty shell including Vite, TS, Tailwind v4, HashRouter, and Framer Motion for cinematic route transitions.

Purpose: Derisk the routing requirements for SCORM delivery and set up the foundation for cinematic transitions without hard loads.
Output: A React app scaffolding that boots, routes via #/ paths, and transitions with Framer Motion.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-engine-proving-de-risking/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Vite + TS + Tailwind React App</name>
  <files>package.json, vite.config.ts, src/main.tsx, src/App.tsx</files>
  <action>
    Initialize the Vite React TS shell. Install Tailwind CSS v4, `react-router` (v7), and `framer-motion`.
    Configure `vite.config.ts` specifically with `base: './'` to satisfy the SCORM packaging requirement of relative paths.
    Clear out default Vite boilerplate in App.tsx and main.tsx.
  </action>
  <verify>
    <automated>npm run build &amp;&amp; grep -q 'base: "\.\/"' vite.config.ts</automated>
  </verify>
  <done>Vite builds successfully with relative assets.</done>
</task>

<task type="auto">
  <name>Task 2: Implement HashRouter & Shell Layout</name>
  <files>src/main.tsx, src/routes.tsx, src/layouts/ShellLayout.tsx</files>
  <action>
    Create a `ShellLayout` with Framer Motion's `AnimatePresence mode="wait"` wrapping an `<Outlet />`.
    In `routes.tsx`, define a `HashRouter` (required for SCORM stability). Map paths: `/start` (StartGate), `/` (Hub), and `/zone/:id` (ZonePlaceholder).
    Inject this router into `main.tsx`. ShellLayout should provide a basic absolute-positioned overlay container suitable for housing the eventual 3D canvas alongside DOM UI.
  </action>
  <verify>
    <automated>grep -q 'HashRouter' src/routes.tsx && grep -q 'AnimatePresence' src/layouts/ShellLayout.tsx</automated>
  </verify>
  <done>Hash router wraps the Animated Outlet.</done>
</task>

<task type="auto">
  <name>Task 3: Build Route Placeholders</name>
  <files>src/components/StartGate.tsx, src/components/Hub.tsx, src/components/ZonePlaceholder.tsx</files>
  <action>
    Create placeholder components matching the core domain boundaries. 
    `StartGate`: A button to "Enter Simulation" (which will later initialize SCORM and satisfy browser autoplay) that navigates to `/`.
    `Hub`: A view recognizing ecosystem.
    `ZonePlaceholder`: A view displaying the route parameter `:id`.
    Wrap their DOM returns in Framer Motion `<motion.div>` with generic fade variants for smooth handoffs.
  </action>
  <verify>
    <automated>grep -q 'motion.div' src/components/StartGate.tsx && grep -q 'motion.div' src/components/Hub.tsx</automated>
  </verify>
  <done>Components render and trigger hash navigation clicks smoothly.</done>
</task>

</tasks>

<verification>
- Vite build passes (`npm run build`).
- Hash routing configured.
- Shell layout correctly integrates Framer Motion AnimatePresence.
</verification>

<success_criteria>
- Developer can start the dev server and click through Start -> Hub -> Zone smoothly.
</success_criteria>

<output>
Create `.planning/phases/01-engine-proving-de-risking/01-01-SUMMARY.md` when done
</output>