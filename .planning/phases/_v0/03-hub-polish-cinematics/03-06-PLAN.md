---
phase: 03-hub-polish-cinematics
plan: "06"
type: execute
wave: 6
depends_on:
  - "03-02"
  - "03-03"
files_modified:
  - src/hooks/useTypewriter.ts
  - src/components/SpeechBubble.tsx
  - src/content/speech-data.ts
  - src/store/useStore.ts
  - src/components/three/CameraRig.tsx
autonomous: false
requirements:
  - P3-SPEECH
  - P3-CAMERA

must_haves:
  truths:
    - "SpeechBubble renders typewriter text character-by-character at 30 chars/sec"
    - "sr-only div with aria-live=polite contains full text immediately for screen readers"
    - "Camera transitions on scene navigation fire dolly to correct zone preset"
    - "Camera dolly completes within 1.5-2.0s with no jitter"
  artifacts:
    - src/hooks/useTypewriter.ts
    - src/components/SpeechBubble.tsx
    - src/content/speech-data.ts
    - src/store/useStore.ts
    - src/components/three/CameraRig.tsx
  key_links:
    - "navigateTo -> setCameraTarget -> CameraRig useEffect -> setLookAt(..., true)"
    - "SpeechBubble reads text from store -> useTypewriter animates -> aria-live announces"
---

## Speech System + Camera Transitions + Integration Checkpoint

<objective>
Enhance SpeechBubble with typewriter animation and aria-live. Create speech-data.ts.
Wire camera transitions to scene navigation. End-to-end integration checkpoint.
</objective>

<execution_context>
@C:/Users/anoop/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/anoop/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-hub-polish-cinematics/03-RESEARCH.md
@.planning/phases/03-hub-polish-cinematics/03-CONTEXT.md
@src/components/SpeechBubble.tsx
@src/components/three/CameraRig.tsx
@src/store/useStore.ts
@src/types.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: useTypewriter hook + speech-data.ts</name>
  <files>src/hooks/useTypewriter.ts, src/content/speech-data.ts</files>
  <behavior>
    - useTypewriter returns { displayedText, isComplete } from text + charsPerSecond
    - Typewriter resets on text change, increments via setInterval at 30 chars/sec
    - speech-data.ts exports Record of Zone to string[] with 2-4 lines per zone
  </behavior>
  <action>
Create src/hooks/useTypewriter.ts: useState for displayedChars, useEffect with setInterval incrementing at charsPerSecond rate (default 30). Return { displayedText: text.slice(0, displayedChars), isComplete: displayedChars >= text.length }.

Create src/content/speech-data.ts: export SPEECH_DATA mapping each Zone to string[] placeholder dialogue matching Wayo tone. act1, zone1, zone2 get 2-4 lines; other zones get empty placeholder arrays.

Per RESEARCH.md Pattern 5: setInterval (not rAF). Per Open Question 4: separate file from component logic.
  </action>
  <verify>
    <automated>npx vitest run -t "typewriter"</automated>
  </verify>
  <done>
useTypewriter hook works. speech-data.ts exports SPEECH_DATA with per-zone arrays. Typewriter test passes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Enhance SpeechBubble + wire camera transitions</name>
  <files>src/components/SpeechBubble.tsx, src/store/useStore.ts, src/components/three/CameraRig.tsx</files>
  <action>
Part A — SpeechBubble (per RESEARCH.md Pattern 5):

New props: text, show, direction (left|center|right), charsPerSecond (default 30), onComplete.
Use useTypewriter hook. Display text.slice(0, displayedChars) with blinking cursor span when !isComplete.
Directional arrow via CSS border trick (Pattern 5). Hidden sr-only div with aria-live="polite" + full text.
Backward compat: children prop renders directly without typewriter. text prop applies typewriter.

Part B — Camera transitions:

Extend navigateTo in useStore.ts: after index lookup, set cameraTarget based on scene zone.
Scenes '16','17' -> zone1; '19','20','22' -> zone2; all others -> hub. Set transitionPhase='transitioning'.
Add CameraControls rest event listener in CameraRig.tsx: when dolly finishes, call useStore.getState().setTransitionPhase('complete'). Use getState() (non-reactive) in the callback.

RESEARCH.md Open Question 2 resolved: IncidentToken orbits during hub idle (Plan 03-03). On zone entry, HubOrbitMap hides (Plan 03-03). Token freezes when component unmounts.
  </action>
  <verify>
    <automated>npx vitest run -t "aria-live"</automated>
  </verify>
  <done>
SpeechBubble shows typewriter text with aria-live. navigateTo triggers camera dolly. transitionPhase updates on dolly complete. Hub rings show/hide correctly per scene zone.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Full Phase 3 integration: global Canvas with orbital map, camera dolly transitions, post-processing stack, LidarCloud in global context, speech bubble with typewriter and aria-live.
  </what-built>
  <how-to-verify>
1. Open app in browser. Confirm F1 broadcast aesthetic (ACES filmic, bloom on rings, vignette, film grain).
2. Confirm single WebGL context: devtools Elements shows exactly one canvas element.
3. Navigate scenes via ProgressDots: camera dollies smoothly (1.5-2s) to correct zone positions.
4. Confirm 3 orbital rings visible on hub/act1 scenes. Rings disappear on zone scenes.
5. Confirm IncidentToken orbits active ring on hub. Token hides on zone entry.
6. Confirm SpeechBubble shows typewriter text with blinking cursor.
7. Confirm aria-live: screen reader announces full text immediately, not character-by-character.
8. Confirm Lidar point cloud visible across all scenes. Jog dial controls cloud reveal.
9. Confirm no console warnings: no setState-in-useFrame, no DOM-in-Canvas, no double tone-mapping.
10. Confirm render stays above 30fps with all effects active (devtools Performance tab).
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| speech-data.ts -> SpeechBubble | Static TS content rendered into DOM. React auto-escapes JSX text. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-06a | Information Disclosure | SpeechBubble XSS | low | mitigate | Speech content from static speech-data.ts (not user input). React auto-escapes JSX text. |
| T-03-06b | Elevation of Privilege | aria-live content injection | low | accept | aria-live reads from same static speech-data.ts source. No untrusted content reaches live region. |
</threat_model>

<verification>
Hub shows rings + cloud + post-processing. Navigate to zone: camera dollies, rings hide, speech appears with typewriter. Screen reader gets full text via aria-live. Single WebGL context. No console warnings.
</verification>

<success_criteria>
Camera transitions work. Speech bubble types with a11y. Hub rings toggle correctly. All P3 requirements met.
</success_criteria>

<output>
Create .planning/phases/03-hub-polish-cinematics/03-06-SUMMARY.md when done
</output>
