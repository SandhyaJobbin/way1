---
phase: 03-hub-polish-cinematics
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - vite.config.ts
  - src/store/useStore.ts
  - src/store/__tests__/useStore.cameraTarget.test.ts
  - vitest.setup.ts
  - src/components/three/__tests__/GlobalScene.test.tsx
  - src/components/three/__tests__/OrbitRing.test.tsx
  - src/components/dom/__tests__/SpeechBubble.test.tsx
  - src/components/three/__tests__/CameraRig.test.tsx
  - src/components/three/__tests__/CloudScene.test.tsx
autonomous: true
requirements:
  - P3-GLOBAL
  - P3-CAMERA
  - P3-INTEGRATE

must_haves:
  truths:
    - "Packages installed: @react-three/postprocessing, postprocessing, @react-spring/three"
    - "Zustand store exposes cameraTarget and transitionPhase state"
    - "Vitest runs in jsdom environment with @testing-library/jest-dom"
    - "All 5 test scaffold files exist with skipped tests"
  artifacts:
    - package.json (3 new R3F deps, 3 new devDeps)
    - vite.config.ts (jsdom env, vitest.setup.ts added)
    - vitest.setup.ts (imports @testing-library/jest-dom)
    - src/store/useStore.ts (cameraTarget, transitionPhase, setters)
    - 5 test scaffold files under __tests__/
  key_links:
    - "zustand cameraTarget -> CameraRig reads it in Plan 03-02"
    - "vitest jsdom env -> React component tests render in Plans 03-02..03-06"
---

## Infrastructure & Test Scaffold

<objective>
Install Phase 3 npm dependencies, extend Zustand store with camera/transition state,
reconfigure Vitest for jsdom component testing, scaffold all 5 test files
from RESEARCH.md validation table (P3-01..P3-08).
</objective>

<execution_context>
@C:/Users/anoop/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/anoop/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-hub-polish-cinematics/03-RESEARCH.md
@.planning/phases/03-hub-polish-cinematics/03-CONTEXT.md
@.planning/ROADMAP.md
@src/store/useStore.ts
@vite.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Phase 3 packages + Vitest jsdom deps</name>
  <files>package.json, package-lock.json</files>
  <action>
Install three core R3F packages (per RESEARCH.md Standard Stack) plus three vitest testing deps:

npm install @react-three/postprocessing@^3.0.5 postprocessing@^6.39.4 @react-spring/three@^10.1.2

npm install --save-dev @testing-library/react@^16 @testing-library/jest-dom@^6 jsdom@^26

BEFORE installing: verify postinstall scripts on each new package via `npm info <pkg> scripts.postinstall`. Per RESEARCH.md Package Legitimacy Audit, all three R3F packages have null postinstall scripts. The testing packages also have no postinstall scripts (standard ecosystem). If any returns non-null, halt and report.

DO NOT install @react-spring/web separately — verify it already exists in package.json (used by JogDial per Phase 2). If missing, add @react-spring/web@^10.1.2.
  </action>
  <verify>
    <automated>node -e "require('@react-three/postprocessing'); require('postprocessing'); require('@react-spring/three'); console.log('OK')"</automated>
  </verify>
  <done>
All 6 packages installed. package.json shows @react-three/postprocessing, postprocessing, @react-spring/three in dependencies; @testing-library/react, @testing-library/jest-dom, jsdom in devDependencies. Node require check passes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Extend Zustand store with camera + transition state</name>
  <files>src/store/useStore.ts, src/store/__tests__/useStore.cameraTarget.test.ts</files>
  <action>
Add to StoreState interface after jogPos field:

  cameraTarget: 'hub' | 'zone1' | 'zone2' | 'zone3';
  transitionPhase: 'idle' | 'transitioning' | 'complete';
  setCameraTarget: (target: 'hub' | 'zone1' | 'zone2' | 'zone3') => void;
  setTransitionPhase: (phase: 'idle' | 'transitioning' | 'complete') => void;

Add to create() initial state after jogPos: 0:

  cameraTarget: computeInitialCameraTarget(),
  transitionPhase: 'idle' as const,

Helper computeInitialCameraTarget() reads SCENE_ORDER[currentSceneIndex]: scenes '16','17' map to 'zone1', scenes '19','20','22' map to 'zone2', all others default to 'hub'. Use SCENE_REGISTRY zone field for mapping.

setCameraTarget sets both cameraTarget and transitionPhase='transitioning'. setTransitionPhase sets only transitionPhase.

Create src/store/__tests__/useStore.cameraTarget.test.ts with 3 tests verifying default is hub, setCameraTarget updates both fields, setTransitionPhase updates only phase. Use existing vitest + zustand testing patterns. Test must pass before task is done.
  </action>
  <verify>
    <automated>npx vitest run -t "cameraTarget"</automated>
  </verify>
  <done>
useStore exports cameraTarget, transitionPhase, setCameraTarget, setTransitionPhase. Unit test passes: default=hub, setCameraTarget(zone1) sets both fields, setTransitionPhase(idle) only changes phase. No existing fields modified.
  </done>
</task>

<task type="auto">
  <name>Task 3: Reconfigure Vitest for jsdom + scaffold 5 test files</name>
  <files>vite.config.ts, vitest.setup.ts, 5 test files in __tests__/</files>
  <action>
A. Update vite.config.ts test block: change environment from 'node' to 'jsdom'. Add setupFiles: ['./vitest.setup.ts']. Keep globals: true and existing include pattern.

B. Create vitest.setup.ts at project root: import '@testing-library/jest-dom';

C. Create 5 test scaffold files. Each file: import target component, one describe block, it.skip blocks tagged with requirement IDs from RESEARCH.md validation table.

File 1: src/components/three/__tests__/GlobalScene.test.tsx
  describe('GlobalScene') — it.skip for P3-01 canvas renders, P3-02 camera transition, P3-08 lidar in global

File 2: src/components/three/__tests__/OrbitRing.test.tsx
  describe('OrbitRing') — it.skip for P3-03 ring colors

File 3: src/components/dom/__tests__/SpeechBubble.test.tsx
  describe('SpeechBubble') — it.skip for P3-05 typewriter, P3-06 aria-live

File 4: src/components/three/__tests__/CameraRig.test.tsx
  describe('CameraRig') — it.skip for P3-02 camera dolly completes within 2s

File 5: src/components/three/__tests__/CloudScene.test.tsx
  describe('CloudScene') — it.skip for P3-08 lidar renders inside global Canvas

Each import uses relative path to component location per RESEARCH.md recommended structure. If component does not exist yet, the import will fail — that is expected and will resolve when the component is created in its plan.
  </action>
  <verify>
    <automated>npx vitest run --reporter=verbose 2>&1 | grep -c "0 passed" && echo "all skipped — ok"</automated>
  </verify>
  <done>
vite.config.ts has environment: 'jsdom' and setupFiles. vitest.setup.ts imports @testing-library/jest-dom. All 5 test files exist with correct describe/it.skip structure. vitest run shows 0 passed, 0 failed (all skipped) — no parse errors.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
None introduced — infrastructure-only plan.

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-SC | Tampering | npm install of new packages | high | mitigate | Verify postinstall scripts null on all packages before install. Per RESEARCH.md, all three R3F packages have null postinstall. Testing packages are standard ecosystem with no postinstall. |
</threat_model>

<verification>
npx vitest run — all existing 27 tests still pass (no regressions). New test file for cameraTarget passes. Scaffold files parse without errors (all tests skipped).
</verification>

<success_criteria>
Packages installed. Store extended. Vitest in jsdom mode. 5 test files scaffolded. Ready for Plan 03-02 tracer.
</success_criteria>

<output>
Create .planning/phases/03-hub-polish-cinematics/03-01-SUMMARY.md when done
</output>
