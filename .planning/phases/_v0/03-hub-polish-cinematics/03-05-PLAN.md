---
phase: 03-hub-polish-cinematics
plan: "05"
type: execute
wave: 5
depends_on:
  - "03-02"
files_modified:
  - src/components/three/CloudScene.tsx
  - src/components/LidarCloud.tsx
  - src/components/three/GlobalScene.tsx
autonomous: true
requirements:
  - P3-INTEGRATE

must_haves:
  truths:
    - "CloudScene renders point cloud inside global Canvas without its own Canvas wrapper"
    - "LidarCloud is a thin DOM wrapper that renders CloudScene (no Canvas)"
    - "CloudScene shader reactivity to jogPos unchanged after extraction"
    - "hasWebGL check moves to App.tsx Canvas init"
  artifacts:
    - src/components/three/CloudScene.tsx (extracted from LidarCloud.tsx)
    - src/components/LidarCloud.tsx (modified: thin wrapper, no Canvas)
    - src/components/three/GlobalScene.tsx (adds CloudScene)
    - src/App.tsx (adds hasWebGL gate)
  key_links:
    - "GlobalScene renders CloudScene + HubOrbitMap + CameraRig + PostProcessing"
    - "CloudScene uses useStore(jogPos) — same reactive pattern as before"
    - "LidarCloud component still exists for backward compat; renders CloudScene without Canvas"
---

## LidarCloud Extraction — Absorb into Global Canvas

<objective>
Extract CloudScene (the points + shader material) from LidarCloud.tsx, removing its internal
Canvas wrapper. CloudScene renders as child of the global Canvas. LidarCloud becomes a thin
wrapper that re-exports CloudScene for any existing consumers. Move hasWebGL() gate to App.tsx.
</objective>

<execution_context>
@C:/Users/anoop/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/anoop/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-hub-polish-cinematics/03-RESEARCH.md (Pitfall 1, Assumption A3)
@src/components/LidarCloud.tsx
@src/components/three/GlobalScene.tsx
@src/App.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract CloudScene — points + shader, no Canvas</name>
  <files>src/components/three/CloudScene.tsx</files>
  <action>
Create new file src/components/three/CloudScene.tsx. Copy the CloudScene function (lines 46-72) from LidarCloud.tsx EXACTLY as-is — do not modify shader code, point generation, or useFrame logic.

Key details from source:
- generateCloud(3000) creates Float32Array of 3000 random points
- vertexShader uses uniforms uTime and uProgress for animation
- fragmentShader creates circular point sprites with cyan color
- useFrame updates uTime from clock.elapsedTime and uProgress from jogPos prop
- useRef for uniforms (NOT useState — correct pattern, avoids Pitfall 2)

EXPORT the CloudScene function as a named export.
EXPORT generateCloud, vertexShader, fragmentShader as named exports (needed for tests).

Per RESEARCH.md Assumption A3: CloudScene uses useStore for jogPos via prop — this is reactive and preserved. No state management change needed. The component was already structured to work inside any Canvas.

CRITICAL: Do NOT wrap in Canvas. Do NOT add Html from drei. This is raw R3F content for the global Canvas.
  </action>
  <verify>
    <automated>npx vitest run -t "lidar in global"</automated>
  </verify>
  <done>
CloudScene.tsx exists with generateCloud, vertexShader, fragmentShader, CloudScene exported. CloudScene function identical to original LidarCloud.tsx lines 46-72. No Canvas wrapper. Test from Plan 03-01 scaffold file parses (test still skipped until wired in Task 3).
  </done>
</task>

<task type="auto">
  <name>Task 2: Update LidarCloud to thin wrapper + move hasWebGL to App</name>
  <files>src/components/LidarCloud.tsx, src/App.tsx</files>
  <action>
A. Rewrite LidarCloud.tsx:

Remove Canvas import. Remove CloudScene function (moved to CloudScene.tsx). Remove generateCloud, vertexShader, fragmentShader (moved). Remove internal hasWebGL check.

New LidarCloud:
- Import CloudScene from './three/CloudScene'
- Keep className prop
- Keep hasWebGL check (moved to top of file or imported from a shared util)
- If WebGL unavailable: render LidarSVGFallback as before
- If WebGL available: render CloudScene directly (NO Canvas wrapper)

LidarCloud becomes:

export function LidarCloud({ className }: { className?: string }) {
  const jogPos = useStore((s) => s.jogPos);
  if (!hasWebGL()) return <LidarSVGFallback className={className} />;
  return <CloudScene jogPos={jogPos} />;
}

This preserves backward compatibility for any existing LidarCloud consumers (scene views) while removing the duplicate Canvas.

B. Move hasWebGL gate to App.tsx:

Add hasWebGL check around the Canvas element. If WebGL unavailable: render LidarSVGFallback fullscreen in the z-0 layer instead of Canvas. Import hasWebGL from LidarCloud or extract to shared util src/lib/webgl.ts.

The Canvas is now conditionally rendered: {hasWebGL() ? <Canvas>...</Canvas> : <LidarSVGFallback className="absolute inset-0" />}
  </action>
  <verify>
    <automated>npm run build 2>&1 | grep -i error || echo "Build passes"</automated>
  </verify>
  <done>
LidarCloud is thin wrapper without Canvas. App.tsx has WebGL gate around Canvas. Build passes — no import errors from CloudScene extraction. Existing LidarCloud consumers still work (they render CloudScene through wrapper which now goes through global Canvas).
  </done>
</task>

<task type="auto">
  <name>Task 3: Wire CloudScene into GlobalScene</name>
  <files>src/components/three/GlobalScene.tsx</files>
  <action>
Add CloudScene to GlobalScene.tsx:

import { CloudScene } from './CloudScene';
const jogPos = useStore((s) => s.jogPos);
Add: <CloudScene jogPos={jogPos} />

CloudScene renders AFTER HubOrbitMap (conditional) and BEFORE PostProcessing. This ensures the point cloud receives post-processing effects.

Full GlobalScene composition after this task:
  ambientLight
  CameraRig
  HubOrbitMap (conditional: cameraTarget === 'hub')
  CloudScene (always visible)
  PostProcessing (always applied)

The CloudScene now renders inside the same WebGL context as the rings and camera — single GPU context, shared post-processing, camera transitions that include the cloud in view.
  </action>
  <verify>
    <automated>npx vitest run -t "lidar in global"</automated>
  </verify>
  <done>
CloudScene renders inside global Canvas. Lidar point cloud visible alongside rings. Jog dial scrubbing still works (jogPos reactively updates CloudScene). Single WebGL context confirmed. P3-08 test in GlobalScene.test.tsx graduates from skip to real assertion.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| App.tsx WebGL gate | hasWebGL() check before Canvas creation — falls back to SVG if WebGL unavailable |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-05a | Denial of Service | WebGL context loss | medium | mitigate | hasWebGL() gate at App.tsx combined with existing LidarSVGFallback. If WebGL lost after init, R3F fires onLost callback — add handler that shows fallback overlay. |
| T-03-05b | Information Disclosure | Point cloud data | low | accept | Point cloud is procedurally generated (Math.random), not loaded from external source. No sensitive data in vertex arrays. |
</threat_model>

<verification>
Single Canvas confirmed. CloudScene renders with point cloud. Jog dial controls cloud reveal. LidarCloud backward compat preserved. hasWebGL gate at App level.
</verification>

<success_criteria>
CloudScene renders in global Canvas. No duplicate Canvas. Jog dial reactivity preserved. WebGL fallback works.
</success_criteria>

<output>
Create .planning/phases/03-hub-polish-cinematics/03-05-SUMMARY.md when done
</output>
