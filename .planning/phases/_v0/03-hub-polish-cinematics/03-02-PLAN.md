---
phase: 03-hub-polish-cinematics
plan: "02"
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified:
  - src/App.tsx
  - src/components/three/GlobalScene.tsx
  - src/components/three/CameraRig.tsx
autonomous: true
requirements:
  - P3-GLOBAL
  - P3-CAMERA

must_haves:
  truths:
    - "Single R3F Canvas wraps entire app, persists across route changes"
    - "DOM overlay (z-10) sits above Canvas (z-0) with existing ShellLayout pattern"
    - "CameraRig reads cameraTarget from Zustand, dollies to preset coordinates"
    - "Camera transition completes within 1.5-2.0s with smooth easing"
  artifacts:
    - src/App.tsx (Canvas wrapper + z-10 DOM overlay)
    - src/components/three/GlobalScene.tsx (scene composition with conditional HubOrbitMap)
    - src/components/three/CameraRig.tsx (CameraControls + preset system)
  key_links:
    - "App.tsx Canvas -> GlobalScene -> CameraRig -> useStore(cameraTarget)"
    - "App.tsx DOM overlay -> SceneRenderer -> existing ShellLayout -> Wayo"
    - "CameraControls.setLookAt(pos, target, true) -> smooth dolly"
---

## Global Canvas Tracer — End-to-End Vertical Slice

<objective>
Wire the thinnest end-to-end 3D path: a single global R3F Canvas wrapping all routes,
a GlobalScene that renders the CameraRig, and a CameraRig that dollies to hub position.
This is the tracer — proves the architecture before horizontal expansion in Plans 03-03..03-06.
</objective>

<execution_context>
@C:/Users/anoop/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/anoop/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-hub-polish-cinematics/03-RESEARCH.md (Pattern 1, Pattern 2)
@.planning/phases/03-hub-polish-cinematics/03-CONTEXT.md
@.planning/STATE.md (ShellLayout z-10 overlay pattern)
@src/App.tsx
@src/store/useStore.ts
@src/routes.tsx
</context>

<tasks>

<task type="tracer">
  <name>End-to-end "Canvas renders + Camera dollies to hub" — one path only</name>
  <files>src/App.tsx, src/components/three/GlobalScene.tsx, src/components/three/CameraRig.tsx</files>
  <action>
TRACER RULE: Wire ONE entry point (App.tsx Canvas) through every layer to the far end (CameraRig dolly to hub). Real error handling, no stubs. One call path only — no zone presets yet, no orbital map, no post-processing.

----------
STEP 1: Modify App.tsx — Add Global Canvas wrapper

Wrap existing content in a fullscreen div structure:

<div className="relative w-screen h-screen overflow-hidden bg-black">
  {/* z-0: Global 3D Canvas */}
  <div className="absolute inset-0 z-0">
    <Canvas
      camera={{ position: [0, 14, 0], fov: 55, up: [0, 0, -1] }}
      gl={{ powerPreference: 'high-performance', antialias: false }}
      style={{ width: '100%', height: '100%' }}
    >
      <GlobalScene />
    </Canvas>
  </div>
  {/* z-10: DOM overlay (preserve existing pattern from STATE.md) */}
  <div className="absolute inset-0 z-10 pointer-events-none">
    <div className="pointer-events-auto w-full h-full">
      <AnimatePresence mode="wait">
        <SceneRenderer key={sceneId} sceneId={sceneId} />
      </AnimatePresence>
    </div>
    <ProgressDots />
  </div>
</div>

Import Canvas from @react-three/fiber. Keep existing AnimatePresence, SceneRenderer, ProgressDots, useStore, SCENE_ORDER imports. The DOM overlay uses pointer-events-none on container + pointer-events-auto on inner div — this allows mouse events to pass through empty areas of the overlay to the Canvas below.

STEP 2: Create GlobalScene.tsx

Minimal scene composition for the tracer — only CameraRig and ambient light:

export function GlobalScene() {
  return (
    <>
      <ambientLight intensity={0.15} />
      <CameraRig />
    </>
  );
}

File: src/components/three/GlobalScene.tsx. Use RESEARCH.md Pattern 2 (Camera PRESETS table) as source for coordinates.

STEP 3: Create CameraRig.tsx

Implement camera dolly system using drei CameraControls + Zustand:

CAMERA_PRESETS map (from RESEARCH.md Pattern 2):
  hub:   { pos: [0, 14, 0],   target: [0, 0, 0] }
  zone1: { pos: [4, 3, -8],   target: [6, 0, -2] }
  zone2: { pos: [-3, 4, -12], target: [-2, 0, -4] }
  zone3: { pos: [-8, 5, -16], target: [-4, 0, -6] }

CameraRig component:
- useRef for CameraControls
- useStore selector for cameraTarget
- useEffect watches cameraTarget, calls controlsRef.current.setLookAt(...preset.pos, ...preset.target, true)
- Returns CameraControls with makeDefault + smoothTime={1.8}
- smoothTime=1.8 gives cinematic 1.8s dolly matching radar-track spec from CONTEXT.md

IMPORTANT per RESEARCH.md Pitfall 4: Do NOT also animate camera via react-spring. CameraControls owns the camera transform. react-spring is for scene objects only (rings, tokens in Plan 03-03).

NO setState in useFrame (RESEARCH.md Pitfall 2). NO DOM elements inside Canvas (Pitfall 3).
  </action>
  <verify>
    <automated>npm run dev -- --host 2>&1 | head -1 || echo "Dev server check; manual: browser should show black bg with no WebGL errors in console"</automated>
  </verify>
  <done>
App starts without WebGL errors. Single Canvas element in DOM (verify via devtools: one canvas element, not two). CameraRig initializes at hub position [0,14,0] looking at [0,0,0]. Setting useStore.setState({ cameraTarget: 'zone1' }) in console dollies camera to zone1 preset in ~1.8s. No DOM-in-Canvas errors, no setState-in-useFrame warnings.
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify tracer — no double WebGL, no crash on route change</name>
  <files>src/App.tsx (verify only — no changes)</files>
  <action>
Manual verification gates for the tracer:

1. Open browser devtools (F12) -> Performance tab -> check GPU memory. Confirm single WebGL context (not two).

2. Navigate between scenes (click ProgressDots or call useStore.navigateTo). Confirm Canvas persists (no flash, no re-initialization). The Canvas element must remain in DOM across all route changes.

3. Check console for warnings: no "setState in useFrame", no "DOM element inside Canvas", no WebGL context lost messages.

4. Call useStore.getState().setCameraTarget('zone2') in console — confirm camera dollies smoothly with no jitter (validates Pitfall 4 avoidance: only CameraControls drives camera, no react-spring on camera).

If any issue found: fix before proceeding to Plan 03-03. This is the architectural gate for the entire phase.
  </action>
  <verify>
    <automated>MISSING — manual verification; check devtools for single canvas element, zero WebGL warnings, smooth camera dolly</automated>
  </verify>
  <done>
Single WebGL context confirmed in devtools. Canvas persists across route changes. No warnings in console. Camera dolly works: setCameraTarget('zone1') -> smooth 1.8s transition to zone1 coordinates.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| App.tsx -> Canvas | R3F creates WebGL context; untrusted shader code (none yet) would execute here |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-02 | Denial of Service | App.tsx Canvas | medium | mitigate | WebGL context loss check — extend hasWebGL() from LidarCloud to global Canvas init. If WebGL unavailable, render LidarSVGFallback in DOM overlay instead of Canvas. |
</threat_model>

<verification>
Dev server starts. Single canvas element. Camera dolly to hub works. Route changes do not recreate Canvas. 5 scaffold tests from Plan 03-01 still parse (still skipped).
</verification>

<success_criteria>
Global Canvas renders. CameraRig dollies to hub. Architecture proven. Ready for horizontal expansion.
</success_criteria>

<output>
Create .planning/phases/03-hub-polish-cinematics/03-02-SUMMARY.md when done
</output>
