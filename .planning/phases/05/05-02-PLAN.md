# 05-02 — Act 1 Intro + Zone 2 Triage Console

**Phase:** 5 — Triage Ops Vertical Slice (The Demo)
**Depends on:** 05-01 (Scenario Content)
**Effort:** M
**Status:** READY

## Goal

Replace Act 1's placeholder speech with a narrative-driven intro sequence and rebuild the Zone 2 triage console (Scene 20) as a fully interactive audit workstation with working JogDial, live lidar viewport, and selectable routing cards.

## Tasks

- [ ] **T1 — Act 1 narrative scenes** `src/views/Scene01.tsx`, `Scene02.tsx`, `Scene03.tsx`
  - Scene 01: Wayo idle, speech bubble delivers autonomy loop explanation (3 lines from ACT1_NARRATIVE), ContinueArrow appears after typewriter completes
  - Scene 02: Wayo curious, low-confidence alert card (confidence: 0.17) pulses into view, speech explains "low-confidence trigger"
  - Scene 03: Wayo concerned, incident card animates from Zone 1 origin to triage queue, speech transitions to "let's examine at the console"
  - Each scene uses `SpeechBubble` with typewriter timing, Wayo emotion state auto-set via `setWayoState`

- [ ] **T2 — Triage console rebuild** `src/views/Scene20.tsx`
  - Left panel: Lidar viewport — SVG sweep animation (existing) + dynamic object count from `TRIAGE_INCIDENT`
  - Center panel: Camera thumbnails 2x2 grid (existing) with live timestamp counter
  - Right panel: Incident metadata panel populated from `TRIAGE_INCIDENT` (id, source, time, type, severity, confidence, sensor fusion tags)
  - Bottom: Timeline scrubber wired to `jogPos` from Zustand — JogDial controls scrubber position bidirectionally
  - Bottom-right: 3 routing cards (face-down) — click to select, selected card flips up showing label + reason + recommended badge
  - Routing selection calls `selectRouting(option)` from triageSlice

- [ ] **T3 — Routing confirmation flow** `src/views/Scene22.tsx`
  - Card flip animation already exists — wire flip to reveal the actually selected option (not hardcoded "Option B")
  - Confirmation card shows "Routed successfully" with selected reason code
  - Orbital map overlay animates token from Ring 2 → Ring 3 using `seamPayload.reasonCode`
  - ContinueArrow triggers `confirmRouting()` then `nextScene()`

## Verification

1. `npm run dev` — Act 1 shows 3-scene narrative with Wayo emotions changing
2. Scene 20: JogDial drag moves timeline scrubber, routing cards are clickable
3. Scene 22: Card flip shows actual selection, orbital map shows correct reason code
4. `npx vitest run` — no regressions
