# 05-03 — The Seam + GitHub Pages Deploy

**Phase:** 5 — Triage Ops Vertical Slice (The Demo)
**Depends on:** 05-02 (Act 1 + Console)
**Effort:** S
**Status:** READY

## Goal

Make the decision state propagation visible — after routing in Zone 2, a "seam" scene shows the incoming Zone 3 queue being populated with the learner's choice. Then deploy the demo to GitHub Pages.

## Tasks

- [ ] **T1 — Seam scene** `src/views/SceneSeam.tsx` (new scene)
  - Insert between Scene 22 (routing confirm) and Scene 16 (Zone 1 placeholder) in `SCENE_ORDER`
  - Shows a Zone 3 "Annotation Workbench" preview panel: incoming queue list with one item populated from `seamPayload`
  - Item displays: incident ID, reason code, routed-from badge ("Triage")
  - Wayo speech: "Your routing decision now feeds the annotation queue — this is the ecosystem in action."
  - Visual: animated card slides from left (Triage ring) into right (Annotation queue slot)
  - Add to `SCENE_REGISTRY` and `routes.tsx`

- [ ] **T2 — Store seam read** `src/store/triageSlice.ts`
  - Add `getSeamPayload()` selector
  - SceneSeam consumes it to populate the incoming queue item

- [ ] **T3 — GitHub Pages deploy** `vite.config.ts` + `.github/workflows/deploy.yml`
  - Set `base: './'` in vite.config.ts (already set per STATE.md)
  - Add `npm run build` output to `dist/`
  - Create deploy workflow: trigger on push to main, build, deploy to gh-pages branch
  - Add `"predeploy": "npm run build"` script to package.json
  - Verify: `npm run build && npx serve dist` — module loads, Act 1 → Zone 2 → Seam flow works

## Verification

1. Full demo flow: Act 1 (3 scenes) → Zone 2 (3 scenes) → Seam → Zone 1 placeholder
2. Routing choice from Scene 22 appears as incoming item in Seam scene
3. `npm run build` succeeds, `dist/` contains index.html + assets
4. GitHub Pages URL loads the module (or local `npx serve dist` confirms)
