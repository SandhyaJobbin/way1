// App.tsx — kept minimal; routing logic lives in routes.tsx
// This file serves as the conceptual root if needed for providers
// Currently, AppRouter in routes.tsx is the top-level component
export default function App() {
  return null
}
